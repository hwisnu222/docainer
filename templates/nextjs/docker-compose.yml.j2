version: "3.8"

services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nextjs-app
    ports:
      - "{{port}}:{{container_port}}"
    environment:
      - NODE_ENV=production
    restart: always
